# Copy to .env.local for local development.
#
# Do not commit secrets. Use your environment/secret manager for real values.

# Database (Story 1.3 will wire this up)
DATABASE_URL=

# Redis / Queue (Epic 3)
REDIS_URL=

# OAuth/Auth provider settings (Epic 1.4+)
NEXTAUTH_URL=
NEXTAUTH_SECRET=

# Local auth bootstrap (Story 1.4)
PRISM_ALLOW_USER_CREATE=false

# Dropbox integration (Epic 3)
DROPBOX_ACCESS_TOKEN=
DROPBOX_ROOT_PATH=

# OCR (Epic 3)
OCR_PROVIDER=noop
DOCUMENT_AI_PROCESSOR_NAME=
PRISM_INTERNAL_WORKER_TOKEN=
PRISM_APP_INTERNAL_URL=http://127.0.0.1:3000

# Extraction (Epic 3)
EXTRACT_PROVIDER=noop
